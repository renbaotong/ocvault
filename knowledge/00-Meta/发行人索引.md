---
created: 2026-04-11
type: index
tags: [索引]
---

# 发行人索引

```dataview
TABLE without id
  file.link as "发行人",
  type as "类型"
FROM "03-发行人基本情况"
WHERE type = "issuer_profile"
SORT file.name ASC
```

## 按区域统计

```dataviewjs
const issuers = dv.pages('"03-发行人基本情况"').where(p => p.type == "issuer_profile");
dv.table(
  ["省份", "数量"],
  Object.entries(
    issuers.groupBy(b => b.province || '未分类')
      .map(g => [g.key, g.rows.length])
  )
);
```

## 索引表

| 发行人全称 | 行政区划 | 行政层级 | 实控人 | 主营业务 | 笔记链接 |
|-----------|---------|---------|--------|---------|---------|
| | | | | | |

## 按业务类型统计

| 业务类型 | 发行人数量 |
|---------|-----------|
| 基础设施 | |
| 土地开发 | |
| 供水/供热 | |
| 其他 | |
